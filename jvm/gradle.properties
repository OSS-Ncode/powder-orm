# Coordinates shared by both JVM artifacts.
# `io.github.oss-ncode` is auto-verified as a Maven Central namespace from the
# GitHub repo (no domain needed). If you later own a domain, switch to it.
group=io.github.oss-ncode
version=0.1.0

org.gradle.jvmargs=-Xmx1g
